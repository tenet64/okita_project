<div class="min-h-screen flex flex-col items-center justify-center bg-gray-100">

  <div class="bg-white shadow-lg rounded-lg px-8 py-10 w-full max-w-md">

    <h1 class="text-center text-2xl font-semibold mb-4">
      パスワード再設定
    </h1>

    <p class="text-center text-sm text-gray-600 mb-6 leading-relaxed">
      登録しているメールアドレスを入力してください。<br>
      パスワード再設定用のリンクをお送りします。
    </p>

    <!-- フラッシュ / エラーメッセージ -->
    <%= render "devise/shared/error_messages", resource: resource %>

    <!-- パスワード再設定フォーム -->
    <%= form_for(resource,
          as: resource_name,
          url: password_path(resource_name),
          html: { method: :post, class: "space-y-4" }) do |f| %>

      <div>
        <label class="text-sm font-medium text-gray-700">
          メールアドレス
        </label>
        <%= f.email_field :email,
              autocomplete: "email",
              class: "border w-full rounded px-3 py-2 mt-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      </div>

      <%= f.submit "再設定メールを送信",
            class: "w-full bg-blue-600 text-white py-2 rounded-md mt-2 hover:bg-blue-700 transition" %>

    <% end %>

    <div class="text-center my-6 text-gray-400 text-sm">
      または
    </div>

    <%= link_to "ログインへ戻る",
          new_user_session_path,
          class: "block text-center text-blue-600 hover:underline text-sm mb-2" %>

    <%= link_to "新規登録",
          new_user_registration_path,
          class: "w-full block text-center bg-gray-800 text-white py-2 rounded-md hover:bg-gray-900 transition" %>

  </div>
</div>