<div class="min-h-screen bg-gray-50 py-10 px-4">
  <div class="max-w-md mx-auto">
    <!-- プロフィールカード -->
    <div class="bg-white rounded-2xl shadow-sm p-6">
      <!-- 情報 -->
      <div class="space-y-4 text-sm text-gray-700">
        <div class="flex justify-between">
          <span class="text-gray-500">ユーザー名</span>
          <span class="font-medium"><%= current_user.name %></span>
        </div>

        <div class="flex justify-between">
          <span class="text-gray-500">メールアドレス</span>
          <span class="font-medium"><%= current_user.email %></span>
        </div>

        <div class="flex justify-between">
          <span class="text-gray-500">登録日</span>
          <span class="font-medium">
            <%= current_user.created_at.strftime("%Y/%m/%d") %>
          </span>
        </div>
      </div>

      <!-- ボタン -->
      <div class="mt-8 flex flex-col gap-3">
        <%= link_to "プロフィールを編集",
          edit_profile_path,
          class: "w-full text-center px-4 py-2 rounded-lg bg-blue-600 text-white font-medium hover:bg-blue-700 transition" %>

        <%= link_to "ダッシュボードへ戻る",
          authenticated_root_path,
          class: "w-full text-center px-4 py-2 rounded-lg bg-gray-100 text-gray-700 hover:bg-gray-200 transition" %>
      </div>
    </div>
  </div>
</div>